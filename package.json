{
  "name": "skbotmessager",
  "version": "1.0.0",
  "private": true,
  "description": "Disparo em massa de SMS e WhatsApp a partir de uma planilha de contatos",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently -n server,web -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix web",
    "start": "npm run build && npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
